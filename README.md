# BOM 扫码质量监管系统

基于 BOM + 总包订单 + 配件订单生成总包/子件标签，出货扫码做齐套校验的局域网系统。

## 两步上手（Windows）

把整个项目文件夹拷到服务器电脑后：

| 步骤 | 操作 | 说明 |
|------|------|------|
| ① 配置环境 | 双击 **`一键配置环境.bat`** | 自动检查/安装 Node.js、配置镜像、安装依赖（首次几分钟） |
| ② 开始运行 | 双击 **`开始运行.bat`** | 启动服务并自动打开浏览器 |

也可双击英文名脚本：`setup-env.bat`（配置）、`run.bat`（运行）。

启动成功后：

- 本机：http://127.0.0.1:3789
- 局域网其他电脑：http://服务器IP:3789

> 窗口不会一闪而过：结束时会提示“请按任意键继续”。关闭「开始运行」窗口 = 停止服务。日常只需再点 **开始运行.bat**；换电脑或依赖损坏时再点一次「一键配置环境」。

### 默认账号

| 用户名 | 密码 | 角色 |
|--------|------|------|
| admin | admin123 | 管理员 |
| import | import123 | 导入打印 |
| scan | scan123 | 扫码入库 |

---

## 功能概览

- 上传 BOM 后自选母件列与母件值（同母件可多版本共存）
- 导入总包订单、配件订单（Excel），可保存列映射规则
- 订单先按自定义列关联总包与配件，再按料号匹配 BOM（不校验用量）
- 齐套数量在扫码时按已关联配件订单行判定
- 匹配成功可生成标签，失败标红待处理
- 可视化拖拽标签模板：支持表格、合并单元格；单元格可选文本/二维码/一维码
- 内容可用订单列点选、任意字符拼接、公式（含 FORMAT 日期等）
- 码内容必须包含系统唯一编号（可再拼接其他内容）
- 扫码：先总包后子件，无顺序、不可重复；齐套后扫下一总包不误报缺漏；支持扫码查单
- 角色：导入打印 / 扫码入库 / 管理员；记录查询与 Excel 导出

## Linux / macOS

```bash
cd bom-scan-qc
npm install
npm start
```

改端口：`PORT=8080 npm start`（Windows：`set PORT=8080 && npm start`）

## 常见问题

| 现象 | 处理 |
|------|------|
| 提示未检测到 Node.js | 再运行一次「一键配置环境.bat」；若自动安装失败，按提示从官网安装后重开脚本 |
| `better-sqlite3` / `node-gyp` / `Visual Studio` 报错 | 请用**最新代码**（依赖已升到 v13，不再强依赖本机编译）。关闭所有窗口 → 删掉 `node_modules` → 再跑「一键配置环境」 |
| `EPERM` 无法删除 node_modules | 关掉杀毒/占用该目录的程序，手动删除 `node_modules` 后再安装 |
| `multer` / `prebuild-install` 的 deprecated 警告 | 可忽略；新版本已升级，警告应消失 |
| 端口被占用 | 关掉已打开的「开始运行」窗口，或换端口启动 |
| 从别的电脑拷了 node_modules | 不要拷贝；在本机重新「一键配置环境」 |

## 推荐使用流程

1. **导入打印员** 上传 BOM → 导入总包/配件订单 → 关联  
2. 调整「标签模板」→「生成打印」  
3. **扫码员**「扫码入库」先扫总包再扫配件；只查订单用「扫码查单」  
4. 「记录报表」查询/导出  

## 数据存储

- SQLite：`data/qc.db`（请定期备份整个 `data` 目录）

## 技术栈

- 后端：Node.js + Express + better-sqlite3 + xlsx  
- 前端：原生 HTML/CSS/JS  
