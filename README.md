# 迅达采购订单批量识别

浏览器端 HTML 工具：批量识别 SAP 迅达采购订单 PDF 的每一条行项目，勾选要导出的字段与行，改表头、写公式后导出 Excel / CSV。

## 导出规则

- **订单行字段**（采购订单号、凭证日期、行项目、物料号、单位、未税单价等）默认导出
- **选配抬头**：供应商、交货地址、付款条件、公司代码、工厂默认关闭；勾选才进入输出表，不勾选不导出。添加关键字「公司代码」等抬头字段会映射到已识别的值
- **导出方案**：把当前勾选的字段、关键字、列顺序、列名和格式存成方案，下次直接应用
- **多行项目**：同一张 PO 的 `00010`、`00020`… 各成一行；描述换行会拼进同一行
- **新字段**：行下新出现的「字段: 值」（如颜色、表面处理）会自动发现为关键字，勾选即可导出
- **可配置关键字**：图号/版本、安装国家、成组技术码、运输类型、D/E、产品家族、SCM大小/量纲。即使样张为空也保留，可勾选导出，也可自行添加关键字
- **行选择**：只输出勾选的识别结果行
- **表头 / 格式**：点击输出表的列头，直接改列名和输出格式
- **列顺序**：拖动表头左侧手柄，或在列格式栏里左移 / 右移；顺序会随方案一起保存
- **公式**：`{字段名}` 取识别值；以 `=` 开头计算，例如 `={数量}*2`、`=IF({数量}>3,"大","小")`、`{物料描述}&" / "&{图号/版本}`

## 使用

```bash
python3 -m http.server 8080
```

打开 http://localhost:8080

- 页面先展示三份样张的预识别结果
- 「加载双行+新字段样张」可核对一张 PO 两行（00010 / 00020）以及新关键字「颜色」「表面处理」
- 也可拖入 / 选择多个 PDF，在浏览器本地识别
- 导出按钮生成当前输出表（含改过的表头与公式结果）

命令行核对样张：

```bash
python3 -m pip install pymupdf
python3 tools/extract_po.py samples/*.pdf -o data/demo.json
python3 tools/test_extract.py
node tools/test_formula.js
node tools/test_fields.js
node tools/test_parser.js
```

## 样张

| 采购订单号 | 行项目 | 物料号 | 物料描述 | 数量 | 金额 | 交货日期 |
| --- | --- | --- | --- | --- | --- | --- |
| 4551750005 | 00010 | 57668963 | Round spot 4LED照明 | 5 | 467.50 | 2026/09/09 |
| 4551750009 | 00010 | 57664581 | LED方型灯 | 4 | 193.20 | 2026/09/15 |
| 4551750012 | 00010 | 57668963 | Round spot 4LED照明 | 2 | 187.00 | 2026/09/08 |
| 4551750099 | 00010 / 00020 | 57668963 / 57664581 | 含换行描述；新字段颜色、表面处理 | 5 + 4 | 660.70 | 2026/09/20 |
